{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "stop_rooms": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "guerra_rooms": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "guerra_private": {
      "$roomId": {
        "$uid": {
          ".read": "auth != null",
          ".write": "auth != null"
        }
      }
    },

    "sombras_rooms": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "sombras_private": {
      "$roomId": {
        "$uid": {
          ".read": "auth != null",
          ".write": "auth != null"
        }
      }
    }
  }
}
